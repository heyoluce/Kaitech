spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kaitechcrm
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    # name: kaitech-crmsystem

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true